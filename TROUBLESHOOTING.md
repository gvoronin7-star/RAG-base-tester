# Устранение неполадок

**Версия:** 2.5.0 (Production Release)

> **Примечание:** Это руководство по устранению неполадок. Основная документация: [README.md](README.md)

**Версия:** 2.5.0 (Production Release)

> **Примечание:** Это руководство по устранению неполадок. Основная документация проекта: [README.md](README.md).

## 📋 Общие проблемы

### Проблема: После команды "5" (Экспорт истории) на экране много мусора и повторов

#### Возможные причины:

1. **Проблема с кодировкой PowerShell**
   - PowerShell может выводить XML-теги (`<Objs>`, `<CLIXML>`)
   - Это создает визуальный "мусор" на экране

2. **Повторный вызов функции**
   - Нажатие Enter несколько раз
   - Автоматический повтор ввода

3. **Особенности терминала**
   - Сохранение истории вывода
   - Буферизация вывода

#### Решения:

##### Решение 1: Используйте правильную кодировку PowerShell

```powershell
# Установите кодировку UTF-8 перед запуском
chcp 65001
$OutputEncoding = [console]::InputEncoding = [console]::OutputEncoding = New-Object System.Text.UTF8Encoding

# Запустите программу
python rag_chat_interactive.py
```

##### Решение 2: Используйте обычный терминал вместо PowerShell

```powershell
# Запустите Command Prompt (cmd.exe)
cmd
python rag_chat_interactive.py
```

##### Решение 3: Проверьте, нет ли повторного вызова функции

Убедитесь, что вы нажимаете Enter только один раз после выбора формата экспорта.

##### Решение 4: Очистите экран перед экспортом

Если проблема сохраняется, добавьте очистку экрана:

```python
import os

# В методе export_history() перед показом меню
os.system('cls' if os.name == 'nt' else 'clear')
```

#### Проверка работоспособности:

Запустите тестовый скрипт для проверки экспорта:

```bash
python test_export.py
```

Если тест проходит успешно, значит проблема не в коде, а в настройках терминала.

---

## 🔧 Другие проблемы

### Проблема: Цвета не отображаются

**Решение:**
```bash
pip install colorama
```

### Проблема: История не сохраняется

**Решение:** Это нормальное поведение. История хранится только в памяти. Используйте экспорт перед выходом.

### Проблема: Ошибка при экспорте

**Решение:** Проверьте права записи в папку `reports/`. Папка создается автоматически.

### Проблема: Скрипты PowerShell не запускаются

**Решение:**
```powershell
# Временное разрешение выполнения скриптов
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process

# Или запуск напрямую
python rag_chat_interactive.py
```

---

## 📞 Поддержка

Если проблема сохраняется:
1. Проверьте версию Python: `python --version` (требуется 3.8+)
2. Проверьте установленные зависимости: `pip list`
3. Запустите тестовый скрипт: `python test_export.py`
4. Опишите проблему с деталями: что именно видите на экране
