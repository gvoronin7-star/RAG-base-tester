# Модуль оценки релевантности для RAG-системы

**Автор модуля:** Koda (AI Assistant) — см. [AUTHORS](AUTHORS)

**Консультант:** Алиса (AI Consultant) — экспертиза по алгоритмам и терминологии

**Версия:** 1.0.0 (2026-02-18)

> **Примечание:** Это подробная документация модуля оценки релевантности. Основная документация проекта: [README.md](README.md).

## 📋 Обзор

Модуль обеспечивает оценку релевантности найденных документов в RAG-системе с использованием косинусного сходства между эмбеддингами запроса и документов.

## 🎯 Основные возможности

- ✅ **Косинусное сходство** — основная метрика релевантности
- ✅ **Фильтрация по порогу** — документы с score < threshold отбрасываются
- ✅ **Сортировка результатов** — документы отсортированы по убыванию релевантности
- ✅ **FAISS индекс** — эффективный поиск по большому количеству векторов
- ✅ **OpenAI API** — стабильная альтернатива PyTorch для эмбеддингов
- ✅ **Обработка крайних случаев** — пустой запрос, пустая база, отсутствие результатов

## 📦 Установка

```bash
pip install -r requirements.txt
```

## 🚀 Быстрый старт

### Базовое использование

```python
from relevance_scorer_openai import RelevanceScorerOpenAI

# Инициализация
scorer = RelevanceScorerOpenAI()

# Документы
docs = [
    "Искусственный интеллект меняет мир технологий.",
    "Машинное обучение — подраздел ИИ.",
    "Python — популярный язык для ИИ."
]

# Поиск
results = scorer.search_relevant_docs(
    query="Как Python связан с ИИ?",
    docs=docs,
    threshold=0.5,
    k=2
)

# Результат: [('Python — популярный язык для ИИ.', 0.65), ...]
print(results)
```

### Интеграция с RAG-системой

```python
from rag_chat import RAGChatSystem

# Инициализация с оценкой релевантности
rag = RAGChatSystem(use_relevance_scorer=True)

# Вопрос с фильтрацией по релевантности
answer = rag.ask_with_relevance(
    question="Какой характер у бенгалов?",
    threshold=0.5,
    top_k=3,
    show_scores=True
)
```

## 🔧 API Reference

### RelevanceScorerOpenAI

#### `__init__(model_name: str = "text-embedding-3-small")`

Инициализация оценщика релевантности.

**Параметры:**
- `model_name`: Название модели OpenAI для эмбеддингов

#### `cosine_similarity(vec1, vec2) -> float`

Вычисление косинусного сходства между двумя векторами.

**Возвращает:** Значение от -1 до 1 (где 1 — полное совпадение)

#### `create_embeddings(texts: List[str]) -> np.ndarray`

Создание эмбеддингов для списка текстов через OpenAI API.

**Параметры:**
- `texts`: Список текстов

**Возвращает:** Матрица эмбеддингов (n_texts, 1536)

#### `build_faiss_index(docs: List[str]) -> faiss.IndexFlatIP`

Построение FAISS индекса для документов.

**Параметры:**
- `docs`: Список документов

**Возвращает:** FAISS индекс

#### `search_relevant_docs(query, docs, faiss_index, threshold, k) -> List[Tuple[str, float]]`

Поиск релевантных документов с оценкой.

**Параметры:**
- `query`: Текстовый запрос
- `docs`: Список документов
- `faiss_index`: FAISS индекс (опционально)
- `threshold`: Порог релевантности (0.0-1.0)
- `k`: Максимальное количество результатов

**Возвращает:** Список кортежей (документ, score), отсортированный по score

## 📊 Как это работает

### Алгоритм

1. **Эмбеддинги документов** — все документы преобразуются в векторы
2. **Нормализация** — векторы нормализуются для корректного косинусного сходства
3. **FAISS индекс** — создается индекс для быстрого поиска
4. **Эмбеддинг запроса** — запрос преобразуется в вектор
5. **Поиск** — FAISS находит k ближайших векторов
6. **Фильтрация** — результаты с score < threshold отбрасываются
7. **Сортировка** — результаты сортируются по убыванию score

### Косинусное сходство

```
score = (A · B) / (||A|| × ||B||)
```

Где:
- A, B — векторы эмбеддингов
- · — скалярное произведение
- ||...|| — норма вектора

**Интерпретация:**
- `1.0` — идеальное совпадение по смыслу
- `0.7-0.9` — высокая релевантность
- `0.5-0.7` — средняя релевантность
- `0.3-0.5` — низкая релевантность
- `<0.3` — отсутствие связи

## 🧪 Тестирование

### Демонстрация с простыми данными

```bash
python demo_relevance_openai.py
```

### Тесты с RAG-системой

```bash
python test_relevance.py
```

### Примеры тестов

#### Тест 1: Релевантный запрос

```python
query = "Какой характер у бенгальских кошек?"
threshold = 0.5

# Результат: 3 чанка с score 0.67-0.69
# Ответ: подробное описание характера
```

#### Тест 2: Нерелевантный запрос

```python
query = "Что такое квантовая физика?"
threshold = 0.6

# Результат: [] (пустой список)
# Сообщение: "Не удалось найти релевантную информацию"
```

#### Тест 3: Высокий порог

```python
query = "Как Python связан с ИИ?"
threshold = 0.9

# Результат: [] (слишком строгий фильтр)
```

## ⚙️ Конфигурация

### Параметры поиска

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `threshold` | float | 0.5 | Порог релевантности (0.0-1.0) |
| `k` | int | 5 | Максимальное количество результатов |
| `show_scores` | bool | True | Показывать ли оценки в выводе |

### Рекомендации по threshold

- **0.3-0.4** — для поиска по широкому кругу тем
- **0.5-0.6** — для большинства запросов (оптимально)
- **0.7-0.8** — для точных запросов с четкой темой
- **0.9+** — для очень специфических запросов

## 📁 Структура файлов

```
.
├── relevance_scorer_openai.py    # Основной модуль (OpenAI API)
├── relevance_scorer.py           # Альтернатива (PyTorch)
├── demo_relevance_openai.py      # Демонстрация OpenAI версии
├── demo_relevance_simple.py      # Демонстрация PyTorch версии
├── rag_chat.py                   # RAG-система с интеграцией
├── test_relevance.py             # Тесты с RAG
└── README_RELEVANCE.md           # Этот файл
```

## 🔍 Примеры использования

### Пример 1: Поиск по документации

```python
docs = [
    "Функция map применяет функцию к каждому элементу списка.",
    "Функция filter фильтрует элементы по условию.",
    "Функция reduce сворачивает список в одно значение."
]

scorer = RelevanceScorerOpenAI()

# Поиск по функциям обработки данных
results = scorer.search_relevant_docs(
    query="Как отфильтровать список?",
    docs=docs,
    threshold=0.6,
    k=2
)

# Результат: [('Функция filter...', 0.85)]
```

### Пример 2: RAG с оценкой релевантности

```python
from rag_chat import RAGChatSystem

rag = RAGChatSystem(use_relevance_scorer=True)

# Вопрос с высокой релевантностью
answer = rag.ask_with_relevance(
    question="Особенности ухода за бенгалами",
    threshold=0.4,  # Средний порог
    top_k=5,
    show_scores=True
)

# Вывод:
# [SCORES] Оценки релевантности:
#    1. Score: 0.72 | Уход за шерстью...
#    2. Score: 0.68 | Питание...
#    ...
```

### Пример 3: Фильтрация по порогу

```python
# Низкий порог — больше результатов
results_low = scorer.search_relevant_docs(
    query="кошки",
    docs=docs,
    threshold=0.3,
    k=10
)
# Результат: 8 документов

# Высокий порог — меньше, но точнее результатов
results_high = scorer.search_relevant_docs(
    query="кошки",
    docs=docs,
    threshold=0.7,
    k=10
)
# Результат: 2 документа
```

## ⚠️ Обработка ошибок

### Крайние случаи

```python
# Пустой запрос
results = scorer.search_relevant_docs("", docs, ..., threshold=0.5)
# Результат: [] + сообщение "[WARN] Пустой запрос"

# Пустая база документов
results = scorer.search_relevant_docs("query", [], ..., threshold=0.5)
# Результат: [] + сообщение "[WARN] База документов пуста"

# Нет документов выше порога
results = scorer.search_relevant_docs("query", docs, ..., threshold=0.99)
# Результат: [] (отфильтрованы все)
```

## 🆚 Сравнение с базовым поиском

| Характеристика | Базовый поиск | С оценкой релевантности |
|----------------|---------------|------------------------|
| Метрика | Евклидово расстояние | Косинусное сходство |
| Фильтрация | Нет | По threshold |
| Score | Нет (только distance) | Да (0.0-1.0) |
| Контроль качества | Нет | Высокий |
| API запросы | 1 (для запроса) | 1 + n (для документов) |

## 💡 Советы по использованию

1. **Начните с threshold=0.5** — оптимальный баланс
2. **Адаптируйте threshold под домен** — разные темы требуют разных порогов
3. **Используйте show_scores=True** при отладке
4. **Мониторируйте API запросы** — оценка релевантности увеличивает количество запросов
5. **Кэшируйте индексы** — для повторных запросов с теми же документами

## 📝 Лицензия

Этот модуль создан для образовательных целей.

## 🤝 Вклад

Для улучшения модуля вы можете:
- Добавить другие метрики сходства (Jaccard, Levenshtein)
- Реализовать кэширование эмбеддингов
- Добавить поддержку других API для эмбеддингов
- Оптимизировать производительность для больших баз данных
